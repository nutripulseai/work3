<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/profile.css">
    <title>Profile</title>
</head>

<header>
    <div class="logo">
        <img src="/imega/doworkoutnow mix.svg" alt="Logo Image">
    </div>
    <div class="menu-icon" id="menu-icon">
        <div class="bar bar1"></div>
        <div class="bar bar2"></div>
        <div class="bar bar3"></div>
    </div>
    <nav id="nav-menu">
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/homeworkout/work">Home Workout</a></li>
            <li><a href="/ai-fitness-app/gptaifitness">AI Fitness App</a></li>
            <li><a href="/menu-builder">menu builder</a></li>
            <li><a href="/moreinformation1">About</a></li>
            <li><a href="/plan">plan</a></li>
            <li><a href="#bmr">BMR</a></li>
            <% if (user) { %>
                <li><a href="/profile" id="settings-button">Profile</a></li>
            <% } %>
            <% if (!user) { %>
                <li class="auth-buttons">
                    <a href="/auth/login" class="btn btn-login">Login</a>
                    <a href="/auth/register" class="btn btn-register">Register</a>
                </li>
            <% } %>
        </ul>
    </nav>
</header>
<body>
  

    <main>
        <h1>Welcome, <%= user.email %>!</h1>
        <p>Email: <%= user.email %></p>
        <p>Subscription Status:
            <% if (user.subscriptionStatus === 'active') { %>
                <span class="status-active">Active</span>
            <% } else if (user.subscriptionStatus === 'expired') { %>
                <span class="status-expired">Expired</span>
                <a href="/purchase-subscription" class="btn btn-renew">Renew Subscription</a>
            <% } else { %>
                <span class="status-free">Free</span>
                <a href="/plan" class="btn btn-subscribe">Subscribe Now</a>
            <% } %>
        </p>
        <a href="/auth/logout" class="btn btn-logout">Logout</a>
    </main>
    
    <script src="/js/profile.js"></script>

    
    
    
</body>
</html>
